<h1>Home page!</h1>

{{#each blogs}}
    <div class="card">
        <h3>{{name}}</h3>
        <h4>Writer: {{User.username}}</h4>
        <h5>Category: {{category_id}}</h5>
        <p>Tags: {{tag_id}}</p>
    </div>
{{/each}}